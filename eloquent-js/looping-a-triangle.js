/* Write a loop that makes seven calls to console.log to output the following
triangle */

let result = '';

for (let i = 0; i < 7; i++) {
  result += '#';
  console.log(result);
}

//other decision wit while lopp
let res = '';
while (res.length < 7) {
  res += '#';
  console.log(res);
}
